import Tag from '../Tag';
import { ParseOptions, TagOptions } from '../type';
declare class Table extends Tag {
    exist_thead: boolean;
    exist_tbody: boolean;
    empty_tbody: boolean;
    constructor(str: string, tagName: string | undefined, options: TagOptions);
    parseValidSubTag(subTagStr: string, subTagName: string, options: ParseOptions): any;
    parseOnlyString(): string;
    beforeReturn(str: string): string;
    exec(prevGap?: string, endGap?: string): string;
}
export default Table;
